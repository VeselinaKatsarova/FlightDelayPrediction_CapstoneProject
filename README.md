# FlightDelayPrediction_CapstoneProject
1.To operate the file you would need to download the dataset located in the link in either one of our relfection Note's files

2. After downloading both the dataset and the file install any needed packages that you may not have. the way to do it is to open R studio and run the install packages line that is at the top of the file (due to the fact that this isnt a python file and requirements.txt doesn't really work for it)

3. Replace our directory that we've used on our computers with your working directory where you want the files to be

4. Run the lines of code separately and not all at once to avoid potential errors due to running too many at once.
